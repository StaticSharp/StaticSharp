
function CheckboxInline(element) {
    Inline(element)

    /*let canvas = document.createElement("canvas")
    element.appendChild(canvas)
    canvas.style.position = "absolute"
    

    function Draw(size, ctx) {
        ctx.fillStyle = "blue"// element.HierarchyBackgroundColor
        ctx.roundRect(0, 0, size, size, size/4);
        ctx.fill();


        //context.ellipse(0, 0, 20, 20, 0, 180, 0)
    }


    new Reaction(() => {
        let size = element.HierarchyFontSize
        canvas.width = size
        canvas.height = size
        let context = canvas.getContext("2d");
        Draw(size, context)
    })*/
}
